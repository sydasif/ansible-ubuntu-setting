---
galaxy_info:
  role_name: setup_netlab
  author: sydasif
  description: Installs NetworkLab via the user's pipx (from the setup_pipx role) with pinned Ansible dependency injection
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
        - resolute
  galaxy_tags:
    - ubuntu
    - desktop
    - networklab
    - network
    - pipx
dependencies:
  - setup_pipx
