---
- name: Copy GNOME wallpaper
  ansible.builtin.copy:
    src: "default_wallpaper.jpg"
    dest: "/usr/share/backgrounds/default_wallpaper.jpg"
    mode: "0644"
    owner: root
    group: root
  become: true

- name: Ensure dconf directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
    owner: root
    group: root
  become: true
  loop:
    - /etc/dconf/profile
    - /etc/dconf/db/local.d
    - /etc/dconf/db/gdm.d

- name: Deploy GNOME dconf defaults
  ansible.builtin.copy:
    src: dconf_gnome.ini
    dest: /etc/dconf/db/local.d/00-gnome-defaults
    owner: root
    group: root
    mode: "0644"
  notify: Update dconf
  become: true

- name: Deploy GDM dconf settings defaults
  ansible.builtin.copy:
    src: dconf_gdm.ini
    dest: /etc/dconf/db/gdm.d/00-global
    mode: "0644"
    owner: root
    group: root
  notify: Update dconf
  become: true

- name: Copy dconf user profile
  ansible.builtin.copy:
    src: profile
    dest: /etc/dconf/profile/user
    mode: "0644"
    owner: root
    group: root
  become: true

- name: Apply current terminal config
  ansible.builtin.shell: |-
    set -e
    if command -v xdg-settings >/dev/null 2>&1; then
      xdg-settings set default-terminal org.gnome.Ptyxis.desktop || true
    fi
    gsettings set org.gnome.desktop.default-applications.terminal exec 'ptyxis' || true
    gsettings set org.gnome.desktop.default-applications.terminal exec-arg '' || true
  become: false
  changed_when: false
