---
- name: Start and enable docker
  ansible.builtin.service:
    name: docker
    enabled: true
    state: started

- name: Restart docker for data-root change
  ansible.builtin.service:
    name: docker
    enabled: true
    state: restarted

- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
