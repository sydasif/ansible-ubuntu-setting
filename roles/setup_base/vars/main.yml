---
# Prerequisites for downstream roles: setup_dotfiles clones a git repo, and
# the starship installer in setup_base curls the upstream script.
dependencies:
  - software-properties-common
  - curl
  - git

cli_packages:
  - bat
  - zsh
  - tree
  - lsd
  - fzf
  - gh

python_packages:
  - python3-venv
