---
# Prerequisite for the starship installer in tasks/main.yml, which curls the
# upstream script. editors/docker/vagrant declare their own copy.
dependencies:
  - software-properties-common
  - curl

cli_packages:
  - bat
  - zsh
  - tree
  - lsd
  - fzf
  - git
  - gh

python_packages:
  - python3-venv
