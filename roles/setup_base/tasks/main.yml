---
- name: Install CLI packages
  ansible.builtin.apt:
    name: "{{ cli_packages }}"
    state: present

- name: Install Python development packages
  ansible.builtin.apt:
    name: "{{ python_packages }}"
    state: present
