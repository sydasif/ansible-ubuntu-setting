---
- name: Install packages
  ansible.builtin.apt:
    name: "{{ dependencies + cli_packages + python_packages }}"
    state: present

- name: Install starship via official script
  ansible.builtin.shell: curl -sS https://starship.rs/install.sh | sh
  args:
    creates: /usr/local/bin/starship
  become: false
